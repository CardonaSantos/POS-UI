/**
 * Interface con value number para react select component
 */
export interface OptionSelected {
  value: number;
  label: string;
}

/**
 * Interface con value number para react select component
 */
export interface OptionSelectedStrings {
  value: string;
  label: string;
}
